---
name: deploy-dashboard
description: Publish a dashboard to its artifact — the publish half of the dashboard's deploy button. Framework page to the standing shareable URL; a map repository's page (only on the user's explicit ask) to its own private artifact, never shared.
disable-model-invocation: true
---

Follow the instructions in `docs/skills/deploy-dashboard.md`.
