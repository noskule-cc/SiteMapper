---
name: deploy-dashboard
description: Publish the framework dashboard to its standing shareable artifact URL — the publish half of the dashboard's deploy button. Framework page only, never a deployment's.
disable-model-invocation: true
---

Follow the instructions in `docs/skills/deploy-dashboard.md`.
